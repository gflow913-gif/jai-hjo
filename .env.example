# Environment Configuration for VS Code Development

# Base application URL (fallback if REPLIT_* vars not present)
BASE_URL=http://localhost:5000

# Production URL (used when NODE_ENV=production and no Replit vars)
PRODUCTION_URL=https://your-production-domain.com

# Cookie domain for authentication (optional, leave unset for localhost development)
# Only set this for multi-subdomain setups with a registrable domain
# COOKIE_DOMAIN=yourdomain.com

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/grow_casino

# Session Secret (generate a random string)
SESSION_SECRET=your-super-secret-session-key-here-make-it-long-and-random

# Authentication Configuration for VS Code (optional - for Google OAuth)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# Discord Bot Configuration (optional)
# DISCORD_TOKEN=your-discord-bot-token-here
# DISCORD_WITHDRAWAL_CHANNEL_ID=1234567890123456789

# Replit-specific configuration (automatically provided in Replit)
# Only needed if running in Replit environment
# REPL_ID=your-repl-id
# ISSUER_URL=https://replit.com/oidc
# REPLIT_DOMAINS=your-domain.replit.dev

# Server Configuration
PORT=5000
NODE_ENV=development